{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npx nodemon src/app.js",
    "client": "cd client && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}